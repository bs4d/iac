#!/bin/sh

exec tail -n +3 $0

menuentry "Windows 11" {
  insmod part_gpt
  insmod fat
  insmod chain
  search --no-floppy --fs-uuid --set=root {{ boot_uuid.stdout }}
  chainloader /EFI/Microsoft/Boot/bootmgfw.efi
}
